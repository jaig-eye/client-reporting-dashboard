# Supabase
# Get from: supabase.com → Project → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Google Ads API
# console.cloud.google.com → Create project → Enable Google Ads API → OAuth credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_DEVELOPER_TOKEN=
GOOGLE_MCC_CUSTOMER_ID=   # digits only

# Meta Marketing API
# developers.facebook.com → Business app → Marketing API
META_APP_ID=
META_APP_SECRET=

# Resend (email for scheduled reports)
# resend.com → API Keys
RESEND_API_KEY=
EMAIL_FROM=reports@youragency.com

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
CRON_SECRET=generate-a-random-secret-here   # Protects /api/cron/sync endpoint

# Admin emails (comma-separated) — only these can access /admin
ADMIN_EMAILS=you@youragency.com,teammate@youragency.com
