// The one sentence for each research field, used verbatim wherever the field appears — the
// wizard, Brand DNA and the Analytics tab — so the same thing never has three names.

/** content_settings.foundational_keywords, shown as "Starting keywords". */
export const STARTING_KEYWORDS_HELP =
  'The 3–10 things this business actually sells. We search outwards from these; we don’t write articles about them directly.'

/** content_settings.research_location. */
export const RESEARCH_LOCATION_HELP =
  'The one city, county or state we measure search demand and rankings in. Leave it empty only for a genuinely national business — otherwise every number you see is country-wide.'

/** content_settings.geographic_focus, next to the research location. */
export const GEOGRAPHIC_FOCUS_HELP =
  'The area in the client’s own words — used in the writing, not for measuring.'
